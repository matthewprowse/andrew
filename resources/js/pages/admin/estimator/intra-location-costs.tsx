import EstimatorLayout from '@/layouts/estimator-layout';

// Cleared for now — same pattern as resources/js/pages/admin/index.tsx.
// Left empty deliberately rather than removed, so the route, layout, and
// sidebar entry keep working.
export default function IntraLocationCostsPage() {
    return <EstimatorLayout title="Intra-Location Costs" />;
}
